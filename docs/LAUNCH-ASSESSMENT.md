# Launch readiness — an actual assessment

Walked on 10 September 2026, against the running stack. Everything below was
CHECKED rather than remembered: the method is named beside each finding so it
can be repeated or disagreed with.

⚠ **Her copy was not touched.** The one exception is the About page, which she
said is still being written and was skipped.

---

## What was checked, and how

| | Method | Result |
|---|---|---|
| Every site page | 85 static routes requested as the operator | 77 × 200; the rest are the 404 page, the 500 page and correct sign-in redirects |
| Every dynamic route | All 19, with real ids and with nonsense | Each renders with real data and 404s on nonsense. Two soft-200s found and fixed (below) |
| Broken links | Crawled from the homepage, 89 pages | None. The only 404s are route TEMPLATES listed by the copy editor (`/classes/[slug]`), which are not links |
| The sitemap | All 103 URLs requested | 103 × 200 |
| Machine endpoints | sitemap, robots, webmanifest, RSS, oEmbed | All correct. oEmbed 400s with no `url` and 200s with one, which is the spec |
| Security headers | Response headers on a page and an API call | CSP `frame-ancestors`, X-Frame-Options, X-Content-Type-Options, Referrer-Policy, Permissions-Policy all present |
| Backend | The whole suite | 762 passing, 14 skipped, 0 failing |
| The app | Host suite + integration suite on a real phone | 53 host tests pass; 84 integration tests pass, with two deliberate refusals (below) |
| App ↔ site | Every `/api/` path in the Dart, against the routers | All 23 exist. **Now a test**, mutation-checked |
| Dev leakage | Grep for localhost / 127.0.0.1 in shipped code | None. Every hit is a comment explaining a guard |

## Fixed while assessing

Not cosmetic; each was live and silent.

1. ⚠ **The Discord bot linked to a page that did not exist.** Every
   announcement it has ever posted carried `{site}/practice/{id}`, and there
   was no such route. Written.
2. ⚠ **`label()` threw on a `covers` it could not parse**, taking down any
   page that rendered that period with a 500.
3. ⚠ **The shared secret was never wired into compose**, so the Discord bridge
   could not run in either direction — the site logged one line per submission
   and carried on.
4. ⚠ **Twelve backend guards had stopped guarding**, failing on a missing file
   rather than on the thing they check. They had been red long enough to read
   as "those always fail".
5. ⚠ **The practice and device models were absent from the metadata**, so an
   autogenerated migration would have written a DROP for both.
6. ⚠ **The app's ephemeris had no Jupiter and no Saturn** — the table stopped
   after Mars, so they were missing from the ephemeris rather than from the
   screen.
7. ⚠ **A route-order shadow** (`/bridge/vote` under `/{work_id}/vote`) caught
   by the guard before it shipped. Fourth time in this codebase; first time
   something other than a person found it.
8. ⚠ **Every period was a page.** `/horoscopes/{sign}/{period}` checked the
   sign and not the period, so `/horoscopes/pisces/fortnightly` came back 200
   titled "Pisces —  — Shruti". An unbounded set of 200s that all say nothing
   is what a crawler eats first. The dated form next door got the check for
   free, so this was the only address it could happen at.
9. ⚠ **Two private pages were indexable**, one of them the shared comparison
   — two people's birth data, drawn. `robots.txt` leaves `/chart/` crawlable
   ON PURPOSE so share links unfurl on Discord and X, which makes `noindex`
   the only thing keeping those pages out of search, and these two did not
   send it. Both halves are now a test.

## ⚠ The running stack was not the code

Worth its own heading, because it invalidated part of this sweep and would
have invalidated the launch.

The site and backend containers had been built **13 hours** before, and
neither mounts its source — they run baked code. The backend was serving 206
routes with **none** of the practice room among them, and the site had no
`/practice/{id}` at all. A first pass over the dynamic routes read as a wall
of failures that were nothing but a stale image.

Both were rebuilt and restarted, and everything below was then re-checked
against the real code. `alembic current` is at `e5c93f27a1d8`, the head.

The lesson for the deploy: **the stack you are looking at is not the code
until you rebuild it**, and it fails by serving an old version rather than by
erroring.

## Two deliberate refusals, not failures

`account_test` and `practice_test` refuse to run unless pointed at a local
stack: they make accounts and post readings, and against the real site that is
junk in her practice room. They fail loudly with the reason. Run them with

    adb reverse tcp:8200 tcp:8200
    flutter test integration_test/practice_test.dart \
      --dart-define=SHRUTI_SITE=http://127.0.0.1:8200

## What launch still needs

**A deploy.** Five migrations, a backend, a site build and a bot restart. The
migrations are additive — new tables and nullable columns — and nothing drops.

**Her three setup tasks.** `SETUP-THREE-THINGS.md`. ⚠ The Firebase package is
`com.shrutivtuber.astrolabe`, corrected in that document today.

**Two settings for the bridge**: `SHRUTI_INTERNAL_SECRET` (`openssl rand -hex
32`, the same value for the site and the bot) and
`SHRUTI_DISCORD_PRACTICE_CHANNEL` (the `#horoscope-practise` id). Empty is a
working state: no bridge, and the room is app-only.

**The terms section.** Written, sitting hidden in Page blocks under
`the-practice-room`. ⚠ Legal wording does not go live because a developer wrote
it — she reads it, changes what she likes, and switches it on.

**The artwork.** Twelve pieces, six essential, in
`design/astrolabe/guidelines/artwork-spec.md`.

**Ten people from the Discord.** ⚠ This is the long pole. Nobody outside has
written a practice reading, redeemed an offer, or received a notification. The
tests say the parts work; they cannot say the thing is good.

## Known and deliberate

- **No iOS.** Android only.
- **The launcher icon is a stand-in** until she draws the clasp.
- **The site is behind the holding page.** A switch.
- **Five site tools are not in the app** — the calendars, the Pañcāṅga, the
  ephemeris page and the geomantic shield. Her decision: they drive traffic to
  the site.
- **No tablet layout**, and the design system says keep it that way.
