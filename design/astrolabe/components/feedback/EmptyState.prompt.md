Nothing here yet — the practice room with no submissions, no saved charts, no offers, nothing written.

```jsx
<EmptyState mark="♄" title="The room is quiet"
  body="Nobody has posted a reading this week. Yours would be the first."
  action={<Button onClick={write}>Write one</Button>} />
```

- Write the copy for this exact state. "No data" is not copy.
- Test with `art` omitted first; that is what ships before her drawings arrive.
