The app's bottom navigation — six tabs, fixed, always visible except inside a full-screen dialog.

```jsx
<TabBar active={tab} badges={{ practice: 2 }} onChange={setTab} />
```

- The hem above the selected tab takes `--ornament`, so it turns rose while she is live.
- Do not add a seventh tab; the next destination is a row in Settings.
