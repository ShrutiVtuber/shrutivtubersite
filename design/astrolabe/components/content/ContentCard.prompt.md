Her latest readings and articles on Home, and the lists behind them.

```jsx
<ContentCard kind="reading" sign="Scorpio" signMark="♏" date="9 Sep" readingTime="2 min"
  excerpt="Mercury is still in the shadow, so the thing you thought was settled…" unread onOpen={open} />
```

- Two-line title clamp, two-line excerpt clamp: a long title never pushes the date off the card.
- The unread dot is an addition to the eyebrow, not a replacement for it.
