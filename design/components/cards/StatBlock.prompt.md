Audience stat for the press kit — large serif numeral (tabular), uppercase label, small note.

```jsx
<StatBlock label="Avg concurrent" value="214" note="past 90 days" glyph="☾" />
<StatBlock label="Followers" value="4.8k" note="Twitch + YouTube" />
```

Format values yourself; keep to 3-4 per row.
