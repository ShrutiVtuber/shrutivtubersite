Journal/VOD pagination — tabular numbers, ← → typographic arrows, ellipsis window past 7 pages. Button mode or static link mode via `hrefFor`.

```jsx
<Pagination page={4} pageCount={12} hrefFor={p=>`/journal/page/${p}`} />
```
