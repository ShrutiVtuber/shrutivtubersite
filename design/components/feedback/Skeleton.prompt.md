Loading skeleton — veil-coloured pulse (opacity only, so reduced-motion collapses cleanly). VideoCard ships its own composite (`VideoCardSkeleton`).

```jsx
<Skeleton variant="rect" height={140} />
<Skeleton variant="text" lines={3} />
<Skeleton variant="circle" width={48} />
```
