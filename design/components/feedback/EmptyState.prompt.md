Authored empty state — dashed hairline field, a moon-phase glyph, quiet honest copy. The schedule's current reality, so it ships designed.

```jsx
<EmptyState glyph="○" title="The sky is quiet"
  body="Nothing scheduled yet — streams are announced on Discord first."
  action={<Button variant="secondary">Join the Discord</Button>} />
```

Use `compact` inside cards/grids (e.g. an empty VOD row).
