Credentials that arrive as files live here, and none of them are committed —
see the patterns in .gitignore. The directory itself is kept because
docker-compose mounts it read-only into the backend, and a missing mount source
is a directory Docker creates for you, silently empty.
