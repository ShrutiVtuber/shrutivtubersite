---
/* The instruments have no index of their own: the venu comes first, and the
   chips on every instrument page reach the other four. */
return Astro.redirect(`/carnatic/instruments/venu${Astro.url.search}`, 302);
---
